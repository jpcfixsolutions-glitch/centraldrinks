# Turso (obligatorio en producción / Vercel)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Auth
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=7d

# Origen del frontend. Varias URLs separadas por coma.
# Los previews *.vercel.app también se permiten automáticamente.
CORS_ORIGIN=http://localhost:5173,https://club-22-vineria.vercel.app

# Solo desarrollo local (Vercel ignora PORT)
PORT=4000
