# Turso (obligatorio en producción / Vercel)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Auth
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=7d

# Origen del frontend (local o URL de Vercel). Varias URLs separadas por coma.
CORS_ORIGIN=http://localhost:5173

# Solo desarrollo local (Vercel ignora PORT)
PORT=4000
