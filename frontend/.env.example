# Desarrollo local (Vite proxea /api al backend en :4000)
# VITE_API_URL=http://localhost:4000/api

# Producción (Vercel, proyecto FRONTEND):
# Obligatoria. Se usa en el build para generar el proxy /api -> backend.
# VITE_API_URL=https://centraldrinks-api.vercel.app/api
